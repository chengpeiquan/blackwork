---
title: Button
description: 用于提交、确认等操作，支持多种样式、尺寸和加载状态。
order: 5
---

# Button

## 适用场景

- 提交表单、确认对话框、开始一项任务
- 工具栏里的图标按钮（`size="icon"`）
- 看起来像按钮的链接（`asChild`）

普通文字导航使用链接即可。`variant="link"` 适合保留按钮行为、以文字链接样式显示的操作。

## 用法

<ButtonBasicExample />

## 示例

### 变体

<ButtonVariantsExample />

### 尺寸

<ButtonSizesExample />

### 加载

`loading` 会禁用按钮并显示加载图标，适合保存或提交期间使用。`loading` 不能与 `asChild` 同时使用。

<ButtonLoadingExample />

### asChild

`asChild` 将按钮样式和属性传给子节点，例如 `Link`。子组件需要接收并传递这些属性。

<ButtonAsChildExample />

<ButtonPropsTable />

## 说明

- 优先通过 `variant` 选择样式，保持按钮颜色一致。
- `size="icon"` 渲染正方形按钮。纯图标按钮需要无障碍名称，例如 `aria-label="搜索"`。
- 表单里的提交按钮请显式写 `type="submit"`。
